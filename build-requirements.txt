# Build requirements for creating the standalone executable
# These are needed in addition to the runtime requirements

# Main build tool
pyinstaller>=5.0

# Runtime dependencies (also needed for building)
asciimatics>=1.15.0
PyYAML>=6.0

# Optional: For better compression
upx-ucl>=3.96
